import { Podcast } from "podcast";

const _feed = new Podcast({
  title: "My Awesome Podcast",
  description: "This is my awesome podcast",
})

export function sum(a: number, b: number): number {
  return a + b;
}